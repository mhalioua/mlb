<ul class="list-group">
  <li class="list-group-item"><%= link_to 'Home', { controller: 'index', action: 'home' }%></li>
</ul>
<table class="table-bordered">
<% @teams.each_with_index do |team, index| %>
  <tr>
    <td><%= index + 1 %></td>
    <td><%= link_to team.stadium, { controller: 'index', action: 'stadium', id: team.id } %></td>
  </tr>
<% end %>
</table>
<br/>
<ul class="list-group">
  <li class="list-group-item"><%= link_to 'Home', { controller: 'index', action: 'home' }%></li>
</ul>